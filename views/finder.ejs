
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>EV Charging Station Finder</title>

    <!-- Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>

    <style>
        /* Minimal CSS for layout */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            padding: 1rem;
            max-width: 1200px;
            margin: auto;
        }

        header {
            background-color: #2c3e50;
            color: white;
            padding: 1rem 0;
        }

        .controls {
            display: flex;
            gap: 1rem;
            margin-bottom: 1rem;
            align-items: center;
        }

        .main-content {
            display: flex;
            gap: 1rem;
        }

        .map-container {
            flex: 2;
            position: relative;
        }

        #map {
            height: 500px;
            width: 100%;
        }

        .stations-sidebar {
            flex: 1;
            background: #f9f9f9;
            padding: 1rem;
            border: 1px solid #ddd;
            max-height: 500px;
            overflow-y: auto;
        }

        .station-card {
            padding: 0.75rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            background: white;
            border-radius: 5px;
        }

        .stations-count {
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .hidden {
            display: none;
        }

        #locate-btn {
            padding: 0.5rem 1rem;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        #locate-btn:disabled {
            background: #aaa;
        }

        select#radius {
            padding: 0.4rem;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>🔌 EV Charging Station Finder</h1>
            <p>Find nearby electric vehicle charging stations</p>
        </div>
    </header>

    <main>
        <div class="container">
            <!-- Controls -->
            <div class="controls">
                <button id="locate-btn">📍 Find My Location</button>
                <label for="radius">Radius:</label>
                <select id="radius">
                    <option value="10">10 km</option>
                    <option value="20">20 km</option>
                    <option value="50" selected>50 km</option>
                    <option value="60">60 km</option>
                    <option value="100">100 km</option>
                </select>
            </div>

            <!-- Main content: map + sidebar -->
            <div class="main-content">
                <div class="map-container">
                    <div id="map"></div>
                </div>

                <div class="stations-sidebar">
                    <h2>Nearby Charging Stations</h2>
                    <div id="stations-count" class="stations-count">No stations found</div>
                    <div id="stations-list" class="stations-list">
                        <div class="no-results">
                            <p>Click "Find My Location" to discover nearby EV charging stations.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Leaflet JS -->
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

    <!-- Your App JS -->
    <script src="/js/script.js"></script>
</body>
</html>
